"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports["default"] = exports.badVersion = void 0;

var badVersion = function badVersion(enforce, current) {
  var LEDGER_VERSION_MAJOR = enforce.major,
      LEDGER_VERSION_MINOR = enforce.minor,
      LEDGER_VERSION_PATCH = enforce.patch;
  var aboveMajor = current.major > LEDGER_VERSION_MAJOR;
  var aboveMinor = current.minor > LEDGER_VERSION_MINOR;
  var abovePatch = current.patch > LEDGER_VERSION_PATCH;
  var atMajor = current.major === LEDGER_VERSION_MAJOR;
  var atMinor = current.minor === LEDGER_VERSION_MINOR;
  var atPatch = current.patch === LEDGER_VERSION_PATCH;
  var belowMajor = current.major < LEDGER_VERSION_MAJOR;
  var belowMinor = current.minor < LEDGER_VERSION_MINOR;
  var belowPatch = current.patch < LEDGER_VERSION_PATCH;
  if (aboveMajor) return false;
  if (belowMajor) return true;

  if (atMajor) {
    if (aboveMinor) return false;
    if (belowMinor) return true;

    if (atMinor) {
      if (abovePatch || atPatch) return false;
      if (belowPatch) return true;
    }
  }

  return true;
};

exports.badVersion = badVersion;
var _default = badVersion;
exports["default"] = _default;
//# sourceMappingURL=badVersion.js.map