import { LotusMessage, SignedLotusMessage } from '@glif/filecoin-message';
import { CoinType } from '@glif/filecoin-address';
import { WalletSubProvider } from '../../wallet-sub-provider';
export declare class SECP256K1KeyProvider implements WalletSubProvider {
    #private;
    mainAddress: string;
    readonly type = "SINGLE_KEY_SECP256K1";
    constructor(privateKey: string);
    getAccounts(_: number, __: number, coinType?: CoinType): Promise<string[]>;
    sign(from: string, message: LotusMessage): Promise<SignedLotusMessage>;
}
//# sourceMappingURL=index.d.ts.map