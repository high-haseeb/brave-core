import * as React from 'react';
import { StyledHeadingTitle, StyledHeadingTitleLabel, StyledSectionHeading, StyledFeatureHeading, StyledH1, StyledH2, StyledH3, StyledH4, StyledH5, StyledH6 } from './style';
class TitleHeading extends React.PureComponent {
    render() {
        const { id, text, label } = this.props;
        return (React.createElement(StyledHeadingTitle, { id: id },
            text,
            label
                ? React.createElement(StyledHeadingTitleLabel, null, label)
                : null));
    }
}
class SectionHeading extends React.PureComponent {
    render() {
        const { id, text } = this.props;
        return (React.createElement(StyledSectionHeading, { id: id }, text));
    }
}
class FeatureHeading extends React.PureComponent {
    render() {
        const { id, text } = this.props;
        return (React.createElement(StyledFeatureHeading, { id: id }, text));
    }
}
class Heading extends React.PureComponent {
    render() {
        const { id, level, customStyle, text } = this.props;
        switch (level) {
            case 1:
                return React.createElement(StyledH1, { id: id, customStyle: customStyle }, text);
            case 2:
                return React.createElement(StyledH2, { id: id, customStyle: customStyle }, text);
            case 3:
                return React.createElement(StyledH3, { id: id, customStyle: customStyle }, text);
            case 4:
                return React.createElement(StyledH4, { id: id, customStyle: customStyle }, text);
            case 5:
                return React.createElement(StyledH5, { id: id, customStyle: customStyle }, text);
            case 6:
                return React.createElement(StyledH6, { id: id, customStyle: customStyle }, text);
            default:
                return React.createElement(StyledH1, { id: id, customStyle: customStyle }, text);
        }
    }
}
export { TitleHeading, SectionHeading, FeatureHeading, Heading };
//# sourceMappingURL=index.js.map