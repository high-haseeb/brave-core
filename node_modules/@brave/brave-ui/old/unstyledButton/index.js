import * as React from 'react';
import StyledUnstyledButton from './style';
class UnstyledButton extends React.PureComponent {
    render() {
        const { id, onClick, customStyle, text } = this.props;
        return (React.createElement(StyledUnstyledButton, { id: id, customStyle: customStyle, onClick: onClick }, text ? text.toString() : null));
    }
}
export default UnstyledButton;
//# sourceMappingURL=index.js.map