import * as React from 'react';
import { StyledMediaContent, StyledMedia, StyledMediaBody } from './style';
class MediaContent extends React.PureComponent {
    render() {
        const { id, media, customStyle, children } = this.props;
        return (React.createElement(StyledMediaContent, { id: id },
            React.createElement(StyledMedia, { src: media, customStyle: customStyle }),
            React.createElement(StyledMediaBody, { customStyle: customStyle }, children)));
    }
}
export default MediaContent;
//# sourceMappingURL=index.js.map