import * as React from 'react';
import 'emptykit.css';
import { StyledPage, StyledPageContent } from './style';
import { TitleHeading } from '../headings/index';
class Page extends React.PureComponent {
    render() {
        const { id, title, label, customStyle, children } = this.props;
        return (React.createElement(StyledPage, { id: id, title: title, label: label, customStyle: customStyle },
            title
                ? React.createElement(TitleHeading, { text: title, label: label })
                : null,
            React.createElement(StyledPageContent, null, children)));
    }
}
export default Page;
//# sourceMappingURL=index.js.map