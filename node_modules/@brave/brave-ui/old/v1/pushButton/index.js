import * as React from 'react';
import { StyledPushButton, StyledPushButtonLink } from './style';
class PushButton extends React.PureComponent {
    render() {
        const { id, color, size, customStyle, onClick, disabled, children } = this.props;
        return (React.createElement(StyledPushButton, { id: id, color: color ? color : 'default', size: size ? size : 'medium', customStyle: customStyle, onClick: onClick, disabled: disabled ? disabled : false }, children));
    }
}
class PushButtonLink extends React.PureComponent {
    render() {
        const { id, color, size, customStyle, href, target, disabled, children } = this.props;
        return (React.createElement(StyledPushButtonLink, { id: id, color: color ? color : 'default', size: size ? size : 'medium', customStyle: customStyle, href: href, target: target, disabled: disabled ? disabled : false, rel: 'noreferrer noopener' }, children));
    }
}
export { PushButton, PushButtonLink };
//# sourceMappingURL=index.js.map