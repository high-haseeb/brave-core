import styled from 'styled-components';
import { setTheme } from '../../../helpers';
const StyledPanel = styled.div `
  max-width: ${(p) => setTheme(p.customStyle, 'maxWidth')};
  min-height: ${(p) => setTheme(p.customStyle, 'minHeight')};
  min-width: ${(p) => setTheme(p.customStyle, 'minWidth')};
  width: ${(p) => setTheme(p.customStyle, 'width')};
  height: ${(p) => setTheme(p.customStyle, 'height')};
  margin: ${(p) => setTheme(p.customStyle, 'margin')};
  padding: ${(p) => setTheme(p.customStyle, 'padding') || '40px'};
  display: ${(p) => setTheme(p.customStyle, 'display')};
  flex-direction: ${(p) => setTheme(p.customStyle, 'flexDirection')};
  justify-content: ${(p) => setTheme(p.customStyle, 'justifyContent')};
  align-items: ${(p) => setTheme(p.customStyle, 'alignItems')};
  background-color: ${(p) => setTheme(p.customStyle, 'backgroundColor') || '#FFF'};
  font-size: inherit;
  box-sizing: border-box;
  border-radius: 20px;
  box-shadow: 0 6px 12px 0 rgba(39, 46, 64, 0.2);
`;
export default StyledPanel;
//# sourceMappingURL=style.js.map