import styled from 'styled-components';
import { setTheme } from '../../../helpers';
const StyledPicture = styled.picture `
  max-width: ${(p) => setTheme(p.customStyle, 'maxWidth')};
  min-width: ${(p) => setTheme(p.customStyle, 'minWidth')};
  width: ${(p) => setTheme(p.customStyle, 'width')};
  height: ${(p) => setTheme(p.customStyle, 'height')};
  min-height: ${(p) => setTheme(p.customStyle, 'minHeight')};
  margin: ${(p) => setTheme(p.customStyle, 'margin')};
  padding: ${(p) => setTheme(p.customStyle, 'padding')};
  box-sizing: border-box;
  display: block;
`;
const StyledFigure = styled.figure `
  box-sizing: border-box;
  display: block;
  max-width: 100%;
  margin: 0;
`;
const StyledFigcaption = StyledFigure.withComponent('figure');
const StyledImage = StyledFigure.withComponent('img');
export { StyledPicture, StyledFigure, StyledFigcaption, StyledImage };
//# sourceMappingURL=style.js.map