import * as React from 'react';
import { StyledPicture, StyledFigure, StyledImage, StyledFigcaption } from './style';
class Image extends React.PureComponent {
    render() {
        const { id, customStyle, children, src, caption } = this.props;
        return (React.createElement(StyledPicture, { id: id, customStyle: customStyle },
            React.createElement(StyledFigure, null,
                children,
                React.createElement(StyledImage, { src: src, customStyle: customStyle })),
            caption && React.createElement(StyledFigcaption, null, caption)));
    }
}
export default Image;
//# sourceMappingURL=index.js.map