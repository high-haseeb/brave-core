import * as React from 'react';
import StyledBoxedContent from './style';
class BoxedContent extends React.PureComponent {
    render() {
        const { id, customStyle, children } = this.props;
        return (React.createElement(StyledBoxedContent, { id: id, customStyle: customStyle }, children));
    }
}
export default BoxedContent;
//# sourceMappingURL=index.js.map