import * as React from 'react';
import { StyledDataBlock, StyledDataItem, StyledDataItemCounter, StyledDataItemText, StyledDataItemDescription } from './style';
class DataBlock extends React.PureComponent {
    render() {
        const { id, asList, children } = this.props;
        return (React.createElement(StyledDataBlock, { id: id, asList: asList }, children));
    }
}
class DataItem extends React.PureComponent {
    render() {
        const { id, customStyle, counter, text, description, onClick, size = 'medium' } = this.props;
        return (React.createElement(StyledDataItem, { id: id, customStyle: customStyle, onClick: onClick, size: size },
            React.createElement(StyledDataItemCounter, { customStyle: customStyle, size: size }, counter),
            text
                ? React.createElement(StyledDataItemText, { customStyle: customStyle, size: size }, text)
                : null,
            description
                ? React.createElement(StyledDataItemDescription, { customStyle: customStyle, size: size }, description)
                : null));
    }
}
export { DataBlock, DataItem };
//# sourceMappingURL=index.js.map