import * as React from 'react';
import StyledPushButton from './style';
class PushButton extends React.PureComponent {
    render() {
        const { id, color, customStyle, onClick, disabled, children } = this.props;
        return (React.createElement(StyledPushButton, { id: id, color: color ? color : 'default', customStyle: customStyle, onClick: onClick, disabled: disabled ? disabled : false }, children));
    }
}
export default PushButton;
//# sourceMappingURL=index.js.map