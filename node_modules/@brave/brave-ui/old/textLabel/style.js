import styled from 'styled-components';
import { setTheme } from '../../helpers';
const StyledTextLabel = styled.p `
  font-weight: ${(p) => setTheme(p.customStyle, 'fontWeight') || 'normal'};
  padding: ${(p) => setTheme(p.customStyle, 'padding') || '0'};
  font-size: ${(p) => setTheme(p.customStyle, 'fontSize') || '13px'};
  color: ${(p) => setTheme(p.customStyle, 'color') || 'inherit'};
  line-height: ${(p) => setTheme(p.customStyle, 'lineHeight')};
  cursor: ${(p) => setTheme(p.customStyle, 'cursor')};
  user-select: none;
  font-family: inherit;
  box-sizing: border-box;
  background: none;
  margin: 0;
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
`;
export default StyledTextLabel;
//# sourceMappingURL=style.js.map