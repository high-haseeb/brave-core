import * as React from 'react';
import StyledTextLabel from './style';
class TextLabel extends React.PureComponent {
    render() {
        const { id, onClick, customStyle, text } = this.props;
        return (React.createElement(StyledTextLabel, { id: id, onClick: onClick, customStyle: customStyle }, text && text.toString()));
    }
}
export default TextLabel;
//# sourceMappingURL=index.js.map