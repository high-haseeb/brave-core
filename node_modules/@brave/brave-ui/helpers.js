export const capitalize = (str) => str.charAt(0).toUpperCase() + str.slice(1);
export const setValueBasedOnSize = (size, medium, small, large) => {
    switch (size) {
        case 'medium':
            return medium;
        case 'small':
            return small;
        case 'large':
            return large;
        default:
            return medium;
    }
};
export const setTheme = (customStyle, prop) => customStyle && customStyle[prop];
let local = {};
export const initLocale = (locals) => {
    local = locals;
};
export const getLocale = (key, replacements) => {
    if (!key || !local[key]) {
        return `MISSING: ${key}`;
    }
    let returnVal = local[key];
    if (!replacements) {
        return returnVal;
    }
    for (let item in replacements) {
        const reString = String.raw `({{\s*${item}\s*}})|(\[\[\s*${item}\s*\]\])`;
        returnVal = returnVal.replace(new RegExp(reString, 'g'), replacements[item].toString());
    }
    return returnVal;
};
//# sourceMappingURL=helpers.js.map