import * as React from 'react';
import { InputComponent, StyledIcon, StyledInput } from './style';
export default class Input extends React.PureComponent {
    render() {
        const { testId, icon, onChange, value, disabled, type, placeholder } = this.props;
        return (React.createElement(InputComponent, { "data-test-id": testId, disabled: disabled },
            icon
                ? React.createElement(StyledIcon, null, icon)
                : null,
            React.createElement(StyledInput, { type: type, onChange: onChange, disabled: disabled, value: value, placeholder: placeholder })));
    }
}
Input.defaultProps = {
    type: 'text'
};
//# sourceMappingURL=index.js.map