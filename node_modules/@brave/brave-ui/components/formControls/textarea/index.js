import * as React from 'react';
import { StyledWrapper, StyledArea } from './style';
export default class TextArea extends React.PureComponent {
    render() {
        const { id, onChange, value, defaultValue, readOnly, placeholder, disabled, fieldError } = this.props;
        return (React.createElement(StyledWrapper, { id: id },
            React.createElement(StyledArea, { onChange: onChange, disabled: disabled, value: value, readOnly: readOnly, placeholder: placeholder, defaultValue: defaultValue, fieldError: fieldError })));
    }
}
//# sourceMappingURL=index.js.map