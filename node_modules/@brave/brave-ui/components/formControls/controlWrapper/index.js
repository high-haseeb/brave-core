import * as React from 'react';
import { StyledWrapper, StyledLabel } from './style';
export default class ControlWrapper extends React.PureComponent {
    render() {
        const { id, text, children, type, disabled } = this.props;
        return (React.createElement(StyledWrapper, { id: id },
            React.createElement(StyledLabel, { type: type, disabled: disabled }, text),
            children));
    }
}
ControlWrapper.defaultProps = {
    type: 'light',
    disabled: false
};
//# sourceMappingURL=index.js.map