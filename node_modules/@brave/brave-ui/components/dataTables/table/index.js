import * as React from 'react';
import { StyledTH, StyledNoContent, StyledTable, StyledTD, StyledTR } from './style';
export default class Table extends React.PureComponent {
    render() {
        const { id, header, rows, children } = this.props;
        return (React.createElement("div", { id: id },
            header && header.length > 0
                ? React.createElement(StyledTable, null,
                    header
                        ? React.createElement("thead", null,
                            React.createElement("tr", null, header.map((cell, i) => {
                                return React.createElement(StyledTH, { key: `${id}-th-${i}`, customStyle: cell.customStyle }, cell.content);
                            })))
                        : null,
                    rows
                        ? React.createElement("tbody", null, rows.map((row, i) => React.createElement(StyledTR, { key: i, customStyle: row.customStyle }, row.content.map((cell, j) => {
                            return React.createElement(StyledTD, { key: `${id}-td-${i}-${j}`, customStyle: cell.customStyle }, cell.content);
                        }))))
                        : null)
                : null,
            !rows || rows.length === 0
                ? React.createElement(StyledNoContent, null, children)
                : null));
    }
}
//# sourceMappingURL=index.js.map