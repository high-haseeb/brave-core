import styled from 'styled-components';
export default function GenerateIconComponentForGraphic(Graphic) {
    return styled.svg.attrs((props) => ({
        children: [Graphic.props.children],
        viewBox: '0 0 32 32',
        'aria-hidden': props.title === undefined ? 'true' : undefined,
        focusable: 'false',
        role: props.title !== undefined ? 'img' : undefined
    })) `
    width: 100%;
    height: 100%;
    fill: currentColor;
  `;
}
//# sourceMappingURL=template.js.map