import * as React from 'react';
import { StyledGrid, StyledColumn } from './style';
export class Grid extends React.PureComponent {
    render() {
        const { id, disabled, columns, customStyle, onClick, children, className } = this.props;
        return (React.createElement(StyledGrid, { id: id, disabled: disabled, className: className, customStyle: customStyle, onClick: onClick, columns: columns }, children));
    }
}
Grid.defaultProps = {
    disabled: false
};
export class Column extends React.PureComponent {
    render() {
        const { id, size, customStyle, children, onClick, className } = this.props;
        return (React.createElement(StyledColumn, { id: id, size: size, className: className, customStyle: customStyle, onClick: onClick }, children));
    }
}
//# sourceMappingURL=index.js.map