import * as React from 'react';
import * as Styled from './style';
export default class Card extends React.Component {
    render() {
        const { testId, children } = this.props;
        const styleProps = {
            className: this.props.className,
            useDefaultContentPadding: this.props.useDefaultContentPadding,
            emphasis: this.props.emphasis
        };
        return (React.createElement(Styled.Card, Object.assign({ "data-test-id": testId }, styleProps),
            React.createElement(Styled.Content, Object.assign({}, styleProps), children)));
    }
}
Card.defaultProps = {
    useDefaultContentPadding: true,
    emphasis: '50'
};
//# sourceMappingURL=index.js.map