const plugin = require("tailwindcss/plugin");

module.exports = plugin(function ({ addComponents, theme }) {
addComponents({
  "hr": {
    "all": "unset",
    "display": "block",
    "borderTop": "1px solid var(--leo-color-divider-subtle)",
    "width": "100%"
  }
});
});