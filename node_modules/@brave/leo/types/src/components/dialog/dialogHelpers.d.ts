/// <reference types="svelte" />
import type { ButtonKind } from '../button/props';
export declare const ensureDialogHelper: () => void;
interface Action {
    result?: any;
    text: string;
    kind?: ButtonKind;
}
interface AlertOptions {
    title: string;
    body?: string;
    actions: Action[];
}
interface AlertInfo extends AlertOptions {
    resolve: (result?: any) => void;
}
export declare const dialogs: import("svelte/store").Writable<AlertInfo[]>;
export declare const dialog: <T>(options: AlertOptions) => Promise<unknown>;
interface AlertDialogOptions {
    okText: string;
}
export declare const alert: (message: string, options: AlertDialogOptions) => Promise<unknown>;
interface ConfirmDialogOptions {
    okText: string;
    cancelText: string;
}
export declare const confirm: (message: string, options: ConfirmDialogOptions) => Promise<boolean>;
export {};
