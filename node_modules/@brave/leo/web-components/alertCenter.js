import SvelteWeb from '../shared/svelte-web.js'
import AlertCenter from '../shared/alertCenter.js'
export default SvelteWeb(AlertCenter, {
    name: 'leo-alertcenter',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/alertCenter.js'