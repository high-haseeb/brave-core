import SvelteWeb from '../shared/svelte-web.js'
import Collapse from '../shared/collapse.js'
export default SvelteWeb(Collapse, {
    name: 'leo-collapse',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/collapse.js'