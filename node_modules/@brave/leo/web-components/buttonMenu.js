import SvelteWeb from '../shared/svelte-web.js'
import ButtonMenu from '../shared/buttonMenu.js'
export default SvelteWeb(ButtonMenu, {
    name: 'leo-buttonmenu',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/buttonMenu.js'