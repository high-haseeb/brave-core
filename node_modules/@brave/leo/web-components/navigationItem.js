import SvelteWeb from '../shared/svelte-web.js'
import NavigationItem from '../shared/navigationItem.js'
export default SvelteWeb(NavigationItem, {
    name: 'leo-navigationitem',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/navigationItem.js'