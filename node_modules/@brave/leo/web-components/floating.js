import SvelteWeb from '../shared/svelte-web.js'
import Floating from '../shared/floating.js'
export default SvelteWeb(Floating, {
    name: 'leo-floating',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/floating.js'