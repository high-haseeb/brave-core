import SvelteWeb from '../shared/svelte-web.js'
import RadioButton from '../shared/radioButton.js'
export default SvelteWeb(RadioButton, {
    name: 'leo-radiobutton',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/radioButton.js'