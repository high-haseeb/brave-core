import SvelteWeb from '../shared/svelte-web.js'
import ProgressRing from '../shared/progressRing.js'
export default SvelteWeb(ProgressRing, {
    name: 'leo-progressring',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/progressRing.js'