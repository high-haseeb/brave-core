import SvelteWeb from '../shared/svelte-web.js'
import Dropdown from '../shared/dropdown.js'
export default SvelteWeb(Dropdown, {
    name: 'leo-dropdown',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/dropdown.js'