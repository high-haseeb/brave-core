import SvelteWeb from '../shared/svelte-web.js'
import NavigationHeader from '../shared/navigationHeader.js'
export default SvelteWeb(NavigationHeader, {
    name: 'leo-navigationheader',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/navigationHeader.js'