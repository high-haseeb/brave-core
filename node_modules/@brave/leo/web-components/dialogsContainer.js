import SvelteWeb from '../shared/svelte-web.js'
import DialogsContainer from '../shared/dialogsContainer.js'
export default SvelteWeb(DialogsContainer, {
    name: 'leo-dialogscontainer',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/dialogsContainer.js'