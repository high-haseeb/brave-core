import SvelteWeb from '../shared/svelte-web.js'
import Dialog from '../shared/dialog.js'
export default SvelteWeb(Dialog, {
    name: 'leo-dialog',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/dialog.js'