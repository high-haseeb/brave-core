import SvelteWeb from '../shared/svelte-web.js'
import FormItem from '../shared/formItem.js'
export default SvelteWeb(FormItem, {
    name: 'leo-formitem',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/formItem.js'