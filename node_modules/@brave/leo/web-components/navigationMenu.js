import SvelteWeb from '../shared/svelte-web.js'
import NavigationMenu from '../shared/navigationMenu.js'
export default SvelteWeb(NavigationMenu, {
    name: 'leo-navigationmenu',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/navigationMenu.js'