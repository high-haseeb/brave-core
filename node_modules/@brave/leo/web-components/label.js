import SvelteWeb from '../shared/svelte-web.js'
import Label from '../shared/label.js'
export default SvelteWeb(Label, {
    name: 'leo-label',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/label.js'