import SvelteWeb from '../shared/svelte-web.js'
import ControlItem from '../shared/controlItem.js'
export default SvelteWeb(ControlItem, {
    name: 'leo-controlitem',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/controlItem.js'