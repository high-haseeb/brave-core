import SvelteWeb from '../shared/svelte-web.js'
import SegmentedControl from '../shared/segmentedControl.js'
export default SvelteWeb(SegmentedControl, {
    name: 'leo-segmentedcontrol',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/segmentedControl.js'