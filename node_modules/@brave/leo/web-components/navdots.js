import SvelteWeb from '../shared/svelte-web.js'
import Navdots from '../shared/navdots.js'
export default SvelteWeb(Navdots, {
    name: 'leo-navdots',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/navdots.js'