import SvelteWeb from '../shared/svelte-web.js'
import NavigationActions from '../shared/navigationActions.js'
export default SvelteWeb(NavigationActions, {
    name: 'leo-navigationactions',
    mode: 'open'
});

// Reexport everything from our component, so consumers can do anything the base
// component can do.
export * from '../shared/navigationActions.js'