import SvelteToReact from '../shared/svelte-react.js'
import AlertCenter from '../web-components/alertCenter.js'
export default SvelteToReact('leo-alertcenter', AlertCenter);

// Reexport everything from our component, so React consumers can do anything
// the base component can do.
export * from '../web-components/alertCenter.js'