import SvelteToReact from '../shared/svelte-react.js'
import NavigationMenu from '../web-components/navigationMenu.js'
export default SvelteToReact('leo-navigationmenu', NavigationMenu);

// Reexport everything from our component, so React consumers can do anything
// the base component can do.
export * from '../web-components/navigationMenu.js'