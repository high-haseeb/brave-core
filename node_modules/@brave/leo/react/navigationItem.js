import SvelteToReact from '../shared/svelte-react.js'
import NavigationItem from '../web-components/navigationItem.js'
export default SvelteToReact('leo-navigationitem', NavigationItem);

// Reexport everything from our component, so React consumers can do anything
// the base component can do.
export * from '../web-components/navigationItem.js'