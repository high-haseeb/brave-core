import SvelteToReact from '../shared/svelte-react.js'
import SegmentedControl from '../web-components/segmentedControl.js'
export default SvelteToReact('leo-segmentedcontrol', SegmentedControl);

// Reexport everything from our component, so React consumers can do anything
// the base component can do.
export * from '../web-components/segmentedControl.js'