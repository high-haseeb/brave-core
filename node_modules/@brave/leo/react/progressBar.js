import SvelteToReact from '../shared/svelte-react.js'
import ProgressBar from '../web-components/progressBar.js'
export default SvelteToReact('leo-progressbar', ProgressBar);

// Reexport everything from our component, so React consumers can do anything
// the base component can do.
export * from '../web-components/progressBar.js'