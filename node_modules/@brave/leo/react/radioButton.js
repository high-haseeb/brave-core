import SvelteToReact from '../shared/svelte-react.js'
import RadioButton from '../web-components/radioButton.js'
export default SvelteToReact('leo-radiobutton', RadioButton);

// Reexport everything from our component, so React consumers can do anything
// the base component can do.
export * from '../web-components/radioButton.js'